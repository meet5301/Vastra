{
  "scripts": {
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix client run dev\" \"npm --prefix admin run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:client": "npm --prefix client run dev",
    "dev:admin": "npm --prefix admin run dev"
  },
  "dependencies": {
    "express": "^5.2.1",
    "react-router-dom": "^7.14.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
